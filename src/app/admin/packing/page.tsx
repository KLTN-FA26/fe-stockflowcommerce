import { PackageCheck } from "lucide-react";
import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function PackingPage() {
  return <ComingSoonPage title="Đóng gói" icon={PackageCheck} />;
}
